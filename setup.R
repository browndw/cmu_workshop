# Run this code to install packages that we'll be using during the workshops.
#
# You ONLY need to do this if you are runniing R and Rstudio on your own laptop.
# If you are using the Binder web interface, these have already been installed
# for you

install.packages(c("tidyverse", "quanteda", "ggrepel"))
devtools::install_github("rlib/rematch2")
